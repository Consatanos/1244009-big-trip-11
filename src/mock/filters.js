/**
 * Array filter names
 */
const filterNames = [
  `everything`,
  `future`,
  `past`,
];

/**
 * Generate filters array
 * @return {array} array of filters
 */
const generateFilters = () => filterNames;

export {
  generateFilters,
};
